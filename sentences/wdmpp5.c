#include <stdio.h>

int wdmpp5Sentence1()
{
	printf( "The programmers took offense to the word lazy, even though they were doing this at the last minute (but only this time, promise).\n" );
	return 1;
}

int wdmpp5Sentence2()
{
	printf( "The Arts and Science building fell catastrophically. No one really missed it though.\n" );
	return 1;
}